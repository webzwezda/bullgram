   3 px '10px' — use a token
   9 px '11px' — use a token
   1 px '12px' — use a token
   1 px '240px' — use a token
   6 px '2px' — use a token
   1 px '52px' — use a token
   1 tailwind-palette 'bg-amber-50' — use a token
   3 tailwind-palette 'bg-indigo-100' — use a token
  12 tailwind-palette 'bg-indigo-50' — use a token
  11 tailwind-palette 'bg-indigo-600' — use a token
   5 tailwind-palette 'bg-indigo-700' — use a token
   7 tailwind-palette 'bg-rose-50' — use a token
   1 tailwind-palette 'bg-rose-600' — use a token
   1 tailwind-palette 'bg-rose-700' — use a token
   9 tailwind-palette 'bg-slate-100' — use a token
   4 tailwind-palette 'bg-slate-200' — use a token
  17 tailwind-palette 'bg-slate-50' — use a token
   1 tailwind-palette 'bg-slate-900' — use a token
   3 tailwind-palette 'border-indigo-100' — use a token
   4 tailwind-palette 'border-indigo-200' — use a token
   1 tailwind-palette 'border-indigo-300' — use a token
   1 tailwind-palette 'border-indigo-400' — use a token
   2 tailwind-palette 'border-indigo-500' — use a token
   2 tailwind-palette 'border-indigo-600' — use a token
   2 tailwind-palette 'border-rose-100' — use a token
   1 tailwind-palette 'border-rose-200' — use a token
  14 tailwind-palette 'border-slate-100' — use a token
  20 tailwind-palette 'border-slate-200' — use a token
   5 tailwind-palette 'border-slate-300' — use a token
   1 tailwind-palette 'divide-slate-100' — use a token
   1 tailwind-palette 'ring-amber-500' — use a token
   1 tailwind-palette 'ring-indigo-200' — use a token
   3 tailwind-palette 'ring-indigo-500' — use a token
   1 tailwind-palette 'ring-indigo-600' — use a token
   5 tailwind-palette 'ring-slate-200' — use a token
   1 tailwind-palette 'shadow-amber-100' — use a token
   2 tailwind-palette 'shadow-indigo-100' — use a token
   4 tailwind-palette 'shadow-indigo-200' — use a token
   1 tailwind-palette 'shadow-indigo-500' — use a token
   1 tailwind-palette 'shadow-rose-200' — use a token
   3 tailwind-palette 'shadow-slate-200' — use a token
   1 tailwind-palette 'text-amber-600' — use a token
  10 tailwind-palette 'text-indigo-500' — use a token
  11 tailwind-palette 'text-indigo-600' — use a token
   3 tailwind-palette 'text-indigo-700' — use a token
   1 tailwind-palette 'text-indigo-900' — use a token
   4 tailwind-palette 'text-rose-500' — use a token
   7 tailwind-palette 'text-rose-600' — use a token
   1 tailwind-palette 'text-rose-700' — use a token
   2 tailwind-palette 'text-slate-400' — use a token
  38 tailwind-palette 'text-slate-500' — use a token
   9 tailwind-palette 'text-slate-600' — use a token
  13 tailwind-palette 'text-slate-700' — use a token
   6 tailwind-palette 'text-slate-800' — use a token
  10 tailwind-palette 'text-slate-900' — use a token
