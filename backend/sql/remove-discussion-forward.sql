-- Миграция 20260926090001: удаление фичи «Пересылать посты в группу обсуждений»
-- и отменённого режима публикации через юзербота.
--
-- Решение владельца (2026-09-26): автопостер публикует ботом (Bot API), юзербота
-- для публикации не используем. Нативную кнопку «Перейти к обсуждению» через Bot API
-- получить нельзя (проверено живым тестом: бот-участник связанной группы + форвард
-- копии тред не создают) — фича пересылки удалена целиком: код, тумблер, колонки.
--
-- Удаляем:
--   channels.discussion_forward_enabled  — тумблер фичи пересылки
--   channels.linked_chat_id              — кэш связанной группы (писался refresh-эндпоинтом)
--   channels.publish_userbot_id          — отменённый режим публикации через юзербота (миграция 20260926090000)
--   autopost_items.discussion_message_ids — id копий постов в группе обсуждений
