-- Миграция 20260918000001 (применена через Supabase MCP 2026-09-18).
-- SQL-копия по конвенции backend/sql/ — схема не должна жить только в проде.
-- Служебный комментарий агента к посту (текст/медиа, batch fan-out включительно).
-- Не рендерится в Telegram: агент оставляет себе контекст поста
-- («это напоминание о привычках, привычки в вики/папка X») и читает его
-- через posts_list, переживая собственные сбросы сессии.
alter table public.autopost_items add column if not exists agent_note text;

comment on column public.autopost_items.agent_note is
    'Служебная заметка агента (не показывается в Telegram): контекст поста. Пишется на все строки batch''а, видна только владельцу через posts_list.';
