# План: экран «Клиенты» (/app/customers)

Дата: 2026-09-13. Статус: принят критиком (ship), добор нит закрыт.

## Раунд 1 — находки критика (5 мажоров, 4 минора, 4 нита)

Закрыто в коммитах `ccf0ce7` + `f41da89` + `d225cd2`:

- **Пустой дефолтный экран:** без `?tab=` normalizedTab был `''` → isBotTab=false → не было
  ни полосы под-сегментов, ни строк, а текст «измените фильтры» врал. Корень: дефолт вкладки.
  Фикс: `normalizeCustomersTab(...) || 'bot'` + честные empty state на каждый из 9 под-сегментов
  (BOT_SUBTABS[].empty); при поиске — прежний «Ничего не найдено».
- **Таблица аудитории:** КЛИЕНТ+ДЕЙСТВИЯ на всю ширину → колонки Имя / Username / TG ID /
  Действия (у платной таблицы + Оплата / Доступ до). Дубль @username в одной ячейке исчез.
- **Группы плиток:** подписи «Воронка клиентов» и «Деньги»; денежные плитки — индиго-полоса слева,
  tabular-nums, при нуле подсказка «Платежей пока нет». Сетки: 5 в ряд / 4 в ряд.
- **«Бот-отец»** → «Бот продаж»; disabled-вкладкам добавлен title с причиной и путём.
- **Миноры/ниты:** plural() хелпер (участник/участника/участников, запись/записи/записей,
  попытки/попыток), синие акценты → indigo (под-сегмент, фокусы, hover самолётика, чип попыток),
  aria-label + «Написать через юзербота» на самолётиках, условный плейсхолдер поиска,
  заголовок таблицы = активный под-сегмент, fade-аффорданс скролла у полосы,
  мёртвый AudienceSection.jsx удалён (содержал «Бот-отец»), getInvoiceStatus упрощён,
  ты/вы и «в contour» нормализованы.

## Уроки

1. **Legacy `.grid` из app.css** (unlayered `grid-template-columns: repeat(auto-fit, minmax(260px,1fr))`)
   перебивает Tailwind-утилиты колонок на любом элементе с классом `grid`. Рабочий приём —
   `[display:grid]` вместо `grid` (так уже сделано в OfficialBotsSection). Диагностика: сетка
   рендерит не те колонки, что в классах, причём число колонок «плывёт» с шириной контейнера.
2. **Проверка свежести бандла:** при проверке деплоя в браузере страница может отдать кешированный
   index.html со старым чанком. Надёжный обход: `fetch('/app/', {cache:'reload'})` + переход по
   URL с уникальным query-параметром; сверять хеш `script[src*=index-]`.
3. При проверке «странного» рендера сначала убедиться, что виден свежий бандл, а не искать баг в коде.
