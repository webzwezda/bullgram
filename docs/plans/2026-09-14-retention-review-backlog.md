# Retention: закрытие бэклага код-ревью (2026-09-14)

Ревью фичи `/app/retention` (code-reviewer сабагент + верификация прод-БД через Supabase MCP) дало вердикт REQUEST_CHANGES. Здесь — решения и статус.

## Правило порядка цикла (уточнение владельца, 2026-09-14)

**Design-critic запускается ТОЛЬКО после код-ревью и его фиксов** — по свежим прод-скриншотам уже исправленной страницы, не параллельно с код-ревью. Не критиковать UI, который сейчас изменится; один рендер-проход вместо двух.

## Решения

- **P0 «напоминание один раз на жизнь подписки»** → новая колонка `subscriptions.last_reminder_sent_at timestamptz`. Условие отправки: `expires_at` в окне [now, now+24h] И (`last_reminder_sent_at` IS NULL OR < now−24h). Продления (in-place) трогать не нужно — джоба самокорректируется. `expiry_reminder_sent` после депоя дропнуть (в коде больше не используется, во фронте не встречается — проверено grep).
- **P1 «флаг до отправки»** → `last_reminder_sent_at` ставится только по финальному исходу: доставлено (бот или юзербот) либо дефинитивный скип (бот заблокирован, фолбэк выключен). Транзиентные ошибки (сеть/таймаут) не маркируют — ретрай следующим тиком. Parse-ошибка Markdown → ретрай plain-text (счистить `*_`[]()~`>#+-=|{}.!`) вместо классификации «бот заблокирован».
- **P1 «кросс-овнер тарифов»** → в retention.job запросы инвойсов/upsell-тарифа скоупятся `tariffs.owner_id = ownerId` (паритет с browse-followup `tariffs!inner(owner_id)`); второй слой — проверка владельца тарифа в `buy_`-хендлере start.handler.js.
- **P2 RLS** → включить RLS + owner-политики на `subscriptions` (EXISTS через channels), `access_events`, `sales_bot_contours`, `payment_settings`. Бэкенд на service-ключе — не затронут; site-v2 эти таблицы напрямую не читает (проверено).
- **P2 re-entrancy** → guard `running` на тике крона.
- **P2 UI-предупреждение** → в блок «Помощник-юзербот» добавить текст про общий чат/знание таргета (эталон UserbotCenterSection.jsx:1421).
- **P3** → `channels!inner` в селекте подписок + ранний continue; `{renewal_link}` без botUsername → убрать строку ссылки; мёртвый тернарник; копи про inline-кнопку; статистика «7 дней» — окно `gte(created_at, now-7d)` + limit 500 вместо 50 последних.

## Секвенция

1. Миграция 1 (до депоя): колонка `last_reminder_sent_at` + партиальный индекс `(expires_at) where status='active'` (аддитивная, старый код не затронет).
2. Код: backend slice (retention.job.js, start.handler.js) + frontend slice (RetentionPage.jsx) — параллельно.
3. Верификация: `npm run build` admin-v2, `node --check`, ревью диффа.
4. Push → CI депой.
5. Миграция 2 (после депоя): дроп `expiry_reminder_sent` + RLS/политики.
6. Smoke прод-страницы + advisors.

## Чеклист

- [x] Миграция 1 применена (`20260914120000`): колонка + индекс; SQL-копия в backend/sql/subscriptions-last-reminder-sent-at.sql
- [x] Backend slice: retention.job.js (P0+P1×2+P2 guard+P3), start.handler.js (owner-чек `buy_`)
- [x] Frontend slice: RetentionPage.jsx (owner-фильтры, предупреждение, P3 косметика/стats)
- [x] Build + ревью диффа (code-reviewer: APPROVE, P2-P3 остатки внесены до коммита: SQL-файл по конвенции, чек ошибки markReminderSent, лог ошибки выборки, `**`→`*` в дефолт-шаблонах legacy Markdown, stripMarkdownDecor для raw-отправки юзерботом + честное превью на странице)
- [x] Push + CI депой (079a01a, run 34766112392: build + pm2 reload + smoke ✓)
- [x] Миграция 2 (`20260914130000`): дроп `expiry_reminder_sent` + RLS/политики на subscriptions (через channels), access_events, sales_bot_contours, payment_settings
- [x] Smoke: /app/retention 200; advisors — 4 таблицы ушли из rls_disabled; expiring-подписок в окне сейчас нет (тик пустой, наблюдение за первым реальным тиком — по access_events)
- [ ] Рантайм-наблюдение (нужен живой подписчик в 24ч-окне): напоминание ровно раз в цикл; `payload->>parse_fallback` на кастомных текстах; визуальная проверка страницы в реальном Chrome (browser doctrine)

## Ревью (после реализации)

- code-reviewer сабагент: **APPROVE**. P0/P1 нет. Остатки P2-P3: (1) in-process re-entrancy guard не защищает мульти-инстанс pm2 — ecosystem без `instances`, один инстанс, ок; (2) после депоя проверить `access_events.payload->>parse_fallback` — если кастомные тексты админов регулярно уходят фолбэком, добавить валидацию Markdown в редакторе страницы; (3) transient-исходы не пишут access_event (сознательно, против спама каждые 5 мин).
- Данные прода проверены: retention_reminder событий ещё нет (0), NULL-ов в sales_bot_contours.owner_id нет — owner-фильтр страницы безопасен.
- Пограничное: «нет бота в реестре + флаг юзербота off» маркируется как дефинитивный скип — паритет со старым поведением, принято.

## Дизайн-бэклаг (волна 2, 2026-09-14)

code-reviewer → design-critic дал REJECT (9 находок: 1 blocker, 4 major, 3 minor, 1 nit). Закрыто коммитом 0c622c7.

Ключевое решение — корневое: легаси-правило `.grid` (auto-fit 260px + margin-top) в app.css было unlayered и перебивало Tailwind v4 утилиты на ВСЕХ страницах админки (сетка статов рендерилась 3+1 вместо 4-up). Удалено вместе с нейтрализатором-симптомом `[data-slot='card-header']`; `.grid` убран из мобильного media-правила. Урок закреплён комментарием-надгробием в app.css: не возвращать голый `.grid` — display:grid даёт сам Tailwind.

Остальное: AA-типографика инструкций (slate-500 + max-w-2xl), хинт про общий чат в привязанном состоянии юзербота, `expires_at` убран из копии, превью «Так увидит подписчик (бот)» с рендером легаси-markdown и чипом inline-кнопки, бейдж подписчиков со склонением, H1 «Удержание» + статусная строка, компактнее стат-карточки, рельс line-clamp-2 + контрастные done-состояния.

Верификация: build ✓, code-reviewer APPROVE (P2 каскадного сдвига разобран по 4 конкретным местам — все после фикса рендерятся по замыслу), прод-рендер в Chrome: сетка 4×237px, H1/превью/бейдж/хинт на месте, sales-bot и рельс без регрессий. Повторная дизайн-критика: **ACCEPT** (9/9 исправлены, новых проблем нет).

Backlog на будущее (не блокирует): стат-карточки с ненулевыми значениями, таблица истории с раскрытой строкой, max-h со скроллом для длинных превью, мобильная ширина и тёмная тема, мёртвый CSS (.grid--double и др.) — отдельная чистка.

## Урок: RUB в продукте нет и не будет (2026-09-14)

Валюты продукта — TON (+ USDT/STARS там, где они уже есть в коде). **RUB-поддержки нет и не планируется** — решение владельца.

- Ревьюверу/агентам: не тратить находки на RUB-флоу — это легаси-ветки (в бэкенде ~29 мест: analytics, referral payouts, crypto-rates TON/RUB, official-bot `manual_rub`-приём чеков и эмодзи валют).
- В бэклаге abandoned из находки P0-2 снята RUB-оговорка («одобрение чека не сверяет сумму для RUB») — сам фикс компаундирующего дисконта остаётся в силе для любой валюты.
- Отдельная задача на чистку (не блокирует): удалить RUB-ветки из бэкенда, тестовый RUB-тариф в проде и ветку `manual_rub` в admin-хендлере, либо явно пометить их как недостижимые. До чистки — не строить на них новую функциональность.

## Бэклаг Abandoned закрыт (волна 3, 2026-09-14, коммит af3ed83)

code-reviewer дал REQUEST_CHANGES: 2×P0, 3×P1, 5×P2, 1×P3. RUB-оговорка снята (см. урок выше). Закрыто конвейером (миграция → параллельные срезы → ревью → правки → депой → прод-проверка).

Ключевые решения:
- **P0-1 «скидка на неоплачиваемый счёт»** → дожим больше не мутирует invoice.amount вовсе; кнопка дожима — новый callback `abbuy_<tariff_id>`: проверка owner+is_active (строже buy_), abandoned-контекст (инвойс этого юзера+тарифа, reminded=true, не paid, <24ч), и создаётся СВЕЖИЙ счёт со скидкой от базовой цены тарифа (max(referral, browse, extra), клэмп 0..99, TTL 15 мин). Без контекста — обычный buy_-флоу. createInvoiceForTariff получил опциональный { extraDiscountPercent }, существующие вызовы не тронуты.
- **P0-2 «компаундирующий дисконт»** → исчез вместе с мутацией; скидка считается от tariffs.price на лету. Parse-фолбэк/stripMarkdownDecor/deliverViaBot вынесены в backend/utils/bot-send.js (новый; retention пока держит свою копию — сводка утилит отдельной задачей). Mark (reminded + reminded_at) только по финальным исходам: delivered / undeliverable / channel_missing / bot_missing / bot_blocked.
- **P1** → awaiting_receipt исключён из дожима; guard тика; юзербот-фолбэк решено НЕ строить (дожим bot-only, страница юзербота не упоминает).
- **P2** → browse-followup: пожизненная «ЗАЩИТА 3» → 7-дневное окно по reminders/reminded_at (миграция 20260914140000 + backend/sql/ копия); лог access_events (browse_followup/browse_reminder); клэмпы; 0% без скидочной строки. Страница: события за 7д limit 500, оба event_type, «в очереди» = только pending (паритет с джобой), «Сбой / пропуск», метки «500+», валидация скидки 0-99, копи без жаргона.
- Ревью-волна поймала P1: browse-события с channel_id=null не проходили фильтр страницы → переключено на owner_id; blocked стал финальным (ретраить некуда — фолбэка нет).

Верификация: node --check ×5, build ✓, ревьювер подтвердил «amount нигде не мутируется», abbuy_ строже buy_, .or() синтаксис ок; прод: страница рендерится, жаргона нет, счётчики на месте. Живой дожим не проверен — в проде 0 pending-инвойсов в окне; первый реальный прогон смотреть по access_events (payload parse_fallback/bot_blocked) и funnel payment_method_selected callback='abbuy'.

Осталось (отдельные задачи): полная чистка RUB-веток (~29 мест + manual_rub), свести bot-send.js с копией в retention.job.js, гонка «оплатил между выборкой и отправкой» (досуществующая, смягчена abbuy_), мобилька/тёмная тема страниц.

## Отдельные задачи закрыты + вторая дизайн-критика (волна 4, коммиты b005ed8, 6abb117)

- **RUB-чистка (safe-скоуп):** блок новых RUB-выплат (whitelist TON/USDT, баланс-ветки), RUB убран из official-bot (порядок/иконки/whitelist наград) и из CoinGecko-запроса (трассировка: единственный читатель RUB-квоты мёртв). Легаси-рендер исторических RUB-данных и manual_rub-приём чеков сознательно оставлены (переплетён с shop-верификацией). В проде констрейнт tariffs_currency_allowed (TON/USDT/STARS, NOT VALID — легаси-строку не ломает, но её редактирование в админке теперь упадёт CHECK'ом; тариф тестовый, задействован в дожим не попадает).
- **Дедуп:** retention.job.js переведён на общий utils/bot-send.js (дубль удалён), util — единственный источник.
- **Гонка оплаты:** ре-чек статуса инвойса перед отправкой дожима; status_changed → skipped + mark.
- Дизайн-критик (второй прогон): abandoned REJECT — 3 замеренных AA-провала (легенда 2.62:1, секционные заголовки 2.62:1, сценарная оговорка 3.41:1), врущая подпись редактора, безымянность; retention без заголовка — держится. Закрыто коммитом 6abb117: slate-600 легенда/оговорка, slate-500 секционные заголовки на обеих страницах, «Общий текст для обоих сценариев», именованный статус «Брошенные корзины» + пилюли, Link в /app/broadcast, превью «Так увидит подписчик (бот)» на abandoned, единый «ОТПРАВЛЕНО ЗА 7 ДНЕЙ».

Критик по retention после фиксов — финальный вердикт ожидается (repair loop, см. session).

**Ре-критика (repair loop): ABANDONED ACCEPT** — все 8 пунктов закрыты и подтверждены замерами (легенда 2.62→7.55:1, оговорка 3.41→7.55:1, секционные заголовки 2.62→4.73:1, якорь и превью на месте); retention без заголовка держится, пара читается как близнецы. Остатки minor: приглушённые нули стат-карточек (намеренный mute-until-alive), ссылка «в рассылку» — indigo добит `!text-indigo` по уроку unlayered a{color:inherit} (4e785cb) + исправлен href на /broadcast относительно basename (84b2561, поймано живой проверкой computed style).

Итог волны 4: отдельные задачи закрыты (RUB safe-чистка + констрейнт, дедуп bot-send, гонка оплаты), обе страницы прошли дизайн-критику. Не закрыто сознательно: manual_rub-флоу (общий с shop), мобилька/тёмная тема (admin десктоп-first), легаси RUB-тариф в проде (не редактировать — CHECK упадёт; зачистить вместе с его инвойсами при следующей чистке данных).

## Бэклаг Referrals закрыт (волна 5, коммиты b3b50ab + 4e785cb/84b2561 префикс)

code-reviewer: REQUEST_CHANGES — 2×P1 (деньги), 3×P2, 2×P3. IDOR/изоляция/жанровые защиты — чисто.

- **P1-1 двойной кредит награды** → атомарный RPC `referral_apply_reward` (миграция 20260914160000): маркер-событие по unique-индексу + инкремент баланса + 2 проводки резерва + converted_at в одной транзакции; duplicate → granted:false. Гонка live/retry и частичные состояния исчезли.
- **P1-2 повторная TON-выплата** → guard 409 из sending/sent, claim только из requested/queued (TOCTOU закрыт пустым result-check); refund: терминальный статус + tx hash СРАЗУ после перевода, ledger идемпотентен chain_tx_unique.
- **P1-инфраструктура (поймал ревью диффа)** → PostgREST экспонирует public-функции: оба RPC закрыты `revoke ... from public, anon, authenticated` + `grant to service_role` (20260914180000); проверено по pg_proc — исполнять могут только postgres/service_role/supabase_admin.
- **P2** → резерв-агрегаты RPC `referral_reserve_summary` (без лимитов), лимиты 500 в GET /api/referrals, DM-кнопка под новый контракт (ops-center + manual_confirmed + known_dialog/common_chat_id + предупреждение про общий чат).
- **P3** → мёртвая RUB-ветка markPayout удалена, тоны статусов сведены к реальным значениям, AA-фиксы контентного текста.

Верификация: обе SQL-функции прогнаны автором на живом Postgres в транзакции с откатом (granted/duplicate/балансы/ledger); индексы в проде на месте; ревью диффа APPROVE (19/19 аргументов RPC, TOCTOU закрыт, логика идентична); прод: jobs стартовали чисто, логи без ошибок, страница рендерится (балансы, пустые состояния, 0 console errors). Живую награду/выплату смотреть по первому реальному конверту атрибуции.

**Инцидент Playwright (см. урок):** мост extension↔server умер молча (все tool calls — таймаут). Починка: убить процесс `npm exec @playwright/mcp` — хост пересоздаёт сервер, тот открывает в Chrome вкладку connect.html, расширение автоконнектится (✅ connected). Кнопки нет в попапе — она во вкладке. Заодно: browser-use в этой среде = только iab, в чужой Chrome не заходит.

## Дизайн-критика Referrals: REJECT → REJECT → ACCEPT (волна 6, ee6e545 + 667efcd + b6e618f)

Первый прогон критика: REJECT (5 major — amber-600 вне волны контраста 3.27-3.38:1, select статуса неотличим от read-only, другой дизайнерский язык чем у retention/abandoned, денежный флоу на window.prompt, hover-only действия). Закрыто за один срез (ee6e545, 11 пунктов).

Ре-критика: 10/11 подтверждены пиксельно, но новый TableEmptyState внёс свежий AA-провал (хинты slate-400 2.62:1 — тот же класс дефекта, с которого начинался цикл) + hasBalance считал RUB + 2 nit. Закрыто (667efcd). Финал: **ACCEPT** — все замеры зелёные (легенда 4.73:1, amber-700 4.97:1, пилюли 7.47:1), пара/тройка страниц читаются в одном языке. Хвост nowrap чипа депозитного перевода добит (b6e618f).

Урок цикла: **каждый новый UI-компонент проходит ту же AA-планку, что и волна фиксов** — TableEmptyState появился в ремонтном PR и тут же упал по контрасту. При вводе компонентов сверяться с текущей шкалой тонов (контентный текст минимум slate-500).

Отдельные задачи на потом: инлайн-формы депозит-флоу и DM-подтверждения (сейчас window.prompt/confirm), red-700 для danger-тона, клик-тест инлайн-формы выплаты с непустыми данными, пагинация GET /api/referrals.

## Бэклаг Bases закрыт (волна 7, коммиты 3144eb5, bc1bec7, 667efcd, b6e618f)

code-reviewer: REQUEST_CHANGES — 2×P1, 5×P2, 1×P3. Изоляция owner_id и приватность по всей цепочке (роуты + broadcast-потребитель + RLS) — чисто.

- **P1-1** ложный 404 при удалении члена базы → читать `count` из `.delete({count:'exact'})` (postgrest-js без .select() отдаёт data=null — подтверждено по коду библиотеки).
- **P1-2** mark-before-send синка аудитории → перевёрнут: present_now не обнуляется заранее; absent присваивается только члену, у которого ВСЕ исходные каналы опрошены успешно (`every`, не `some` — поправлено после ревью); ответ дополнен failed_channels/truncated/total_count.
- **P2** → экранирование поиска двойными кавычками (бэкслеши PostgREST не поддерживает — ревьюер проверил живьём, PGRST100), owner-скоуп channel_audience_channels через base_id, честные капы (total_count/payments_truncated/truncated по TotalList.total), CTA /app/sales-bot, корзина импорта переживает неудачу (pending-долив + sessionStorage персист), AA-контентный текст slate-500.
- **P3** → мёртвый import-subscriptions удалён, кап 5000 на POST members, валидация tg_user_id + lowercase-канонизация @username, skipped в тостах.

Дизайн-критика: REJECT (3 major — нули статов 2.57:1, инверсия «заголовок светлее тела» 4.73<7.55, двойной тост корзины) → фиксы (bc1bec7, 10 пунктов) → ре-критика **ACCEPT**: нули 4.64:1, заголовки 10.28:1, тосты взаимоисключающие, регрессий нет. Урок: новый компонент TableEmptyState в ремонтном PR сам упал по AA — правило «контентный текст минимум slate-500» теперь и для хинтов.

Отдельные задачи на потом: pg-пагинация членов/справочников, инлайн-формы депозит-флоу, thead-паритет проверен на всех четырёх страницах, клик-тест тостов корзины на живых данных.

## Студийный проход критика по четвёрке (волна 8, 4262166 + 7941b3c)

Критик прогнал все четыре страницы (retention/abandoned/referrals/bases) попиксельно: бэклог исполнен (микролейблы referrals, инлайн-панель возврата, red-700, нули, max-h-72, indigo-ссылка, TableEmptyState), студия «впервые читается как один продукт» (единый ink микролейблов #66748c, одна анатомия стат-карточки, одна грамматика пустых состояний).

Условные ACCEPT закрыты: **токен нулей зафиксирован правилом** — «деятельные» нули (счётчики событий) глухие slate-500; «денежные» нули (резерв/балансы) тёмные slate-900, потому что ноль денег — информация, а не пустота (bases «ДОСТУПНО 0 TON» тёмным уже был принят). По правилу исправлены первые карточки retention/abandoned (7941b3c). Ложная тревога критика по «TON-резерва» — в коде грамматически верный «TON-резерв».

Итог аудита студии: 4 страницы — код-ревью APPROVE, дизайн ACCEPT, прод чистый. Остатки вне цикла: dark/мобилька (вне решений владельца), клик-тесты на живых данных, пагинация, зачистка легаси RUB-тарифа.

## Рантайм-верификация /app/bases (живой Chrome, 2026-09-14)

Все оставленные ревьювером и критиком клик-тесты выполнены на проде:
- Поиск с запятой («Смирнов, Иван») → 200, `{"members":[],"total":0}` — экранирование работает, 500 нет; контрольный «webzwezda» → ровно 1 строка.
- «В базу» при выбранной базе → добавление прямо в базу (корзина — только для режима новой базы). Тройной POST одного члена → ровно 1 строка в БД (upsert base_id,tg_user_id — дубликатов нет).
- **P1-1 живьём:** «Удалить из базы» → success-тост «Удалён из базы», без ложного 404, без выкидывания из базы; БД вернулась к исходным 3 членам.
- **Тосты корзины (хвост критика):** первый клик → буфер 1; второй клик по тому же → буфер 1 + единственный тост «Уже в корзине» (двойного тоста и StrictMode-дубля нет).
Тестовые данные за собой убраны (член удалён, корзина очищена).

## UX-волна Bases: «владелец не может разобраться» (3d495b8, design-review skill)

UX-walkthrough владельца в Chrome подтвердил три корневых дефекта: (1) телепорт в первую базу при заходе без ?base; (2) кнопка «В базу» ведёт себя по-разному (прямое добавление vs корзина) в зависимости от скрытого режима, переключатель которого живёт в другой карточке; (3) нет нейтрального состояния редактора.

Фиксы (3d495b8): телепорт убран (нейтральная точка входа: «Выберите базу из списка или создайте новую» + кнопка), адресат «В базу» стал видимым — чип «Куда попадёт «В базу»: …» в шапке таблицы аудитории (обновляется по режиму), честный тост при клике без выбранной базы. Verified в проде: чистый URL на входе, чип «База «курс»» при выборе, сценарии пройдены.

Design-review оценка (после фиксов): Hierarchy 8, Consistency 8, Accessibility 8, Usability 8, Responsiveness 6, Performance 8 → **7.8/10**. Было по walkthrough: Usability 4, Hierarchy 6.

Правило студии зафиксировано: «деятельные» нули (счётчики событий) — slate-500; «денежные» нули (резерв/балансы) — slate-900 (ноль денег — информация). Новый UI-компонент проходит ту же AA-планку, что и волна фиксов.

## Бэклаг Customers закрыт (волна 9, коммиты 483c91d + 6c34753 + 40dc528)

Порядок цикла изменён владельцем: **design-critic — только после код-ревью и фиксов**, по свежим прод-скриншотам (правило в начале документа). Первая страница, прошедшая новый порядок.

code-reviewer: REQUEST_CHANGES — 1×P1, 4×P2, 5×P3. owner_id/изоляция по всем роутам (включая смежные analytics/audience/userbot CRM) — чисто. Ревью диффа поймало P1 в моём же контракте капов: total среза таблицы ≠ счётчик подмножества вкладки («1200+ записей» над таблицей из 50 активных) — плюс P2 (throw в batch-цикле превращал частичный успех в 500) и P3 (money-path: throw из upsert после оплаты рвал выдачу покупателю).

Ключевое:
- **P1 analytics**: фулл-скан всех инвойсов проекта (поллинг каждые 60с) → owner-scoped `tariffIds` + `.in('tariff_id', …)` + `tariffs!inner` (паттерн customers.routes.js:305); форма ответа /api/analytics не тронута, RUB-легаси не тронут.
- **caps-контракт**: `{funnel,invoices,subscriptions}×{limit,total,truncated}` через `count:'exact'`; фронт — честная нижняя граница «N+ записей · показаны последние N» (НЕ total среза — он не равен подмножеству вкладки после дедупа/статусов), капа отключается при активном поиске/фильтре (total считается без фильтра). Урок: **для честных меток урезанных выборок — либо per-segment count, либо нижняя граница «N+»; total общего среза подмешивать к счётчику подмножества нельзя**.
- **Ошибки**: `isUndefinedTableError` (42P01 + 'does not exist') вместо `includes('имя_таблицы')` в 3 местах; проглоченное — console.error. PGRST205 сознательно не глотается (падает громко; таблицы в проде существуют).
- **TOCTOU продлений**: update с `.eq('expires_at', прочитанное)`/`.is(null)`, конфликт → перечитать и ретрай (до 3 попыток); batch-add-days — построчный `failed` вместо throw (200 с {updated, conflicts, failed}); money-path `upsertSubscriptionForChannel` — после исчерпания ретраев фолбэк-запись без гарда (деньги уже уплачены — доступ важнее теоретической потери параллельного продления) + console.error для компенсации.
- **days** валидация: 'forever' или целое 1..3650, иначе 400 (было NaN → 500). Single-роут :4138/:4477 с тем же багом — отдельным слайсом.
- **AA/копи**: заголовки таблиц, лейблы карточек, TG ID — slate-500; ненулевой «Доступ закончился» red-500→slate-900 (3.76:1 + домовой паттерн); денежный ноль slate-900 «За период платежей нет»; reqId-гард поллинга в loadCustomers (включая catch).

Дизайн-критика (после фиксов): REJECT — 1 major (главное число карточки `text-emerald-500` = 2.47:1 < 3:1 large) + 3 minor («ПРИЧИНА» truncate до «Выда…», неактивные пилюли slate-500 на bg-slate-100 = 4.35:1, disabled-вкладки slate-300 только по светлости) + 5 nit. Фиксы (40dc528, 4 строки) → ре-критика **ACCEPT**: emerald-600 ~3.2:1 (v4-oklch; в коммит-сообщении ошибочно 3.65 из v3-математики — запас 1%, число не уменьшать), пилюли 6.82:1, line-clamp-2 без каскадов, disabled в домовом opacity-50. Новых дефектов ремонта нет.

Остатки (не блокируют): single add-days :4138/:4477 (тот же parseInt/гард-баг); nit-хвост критика — TableEmptyState-рефактор пустых состояний, три грамматики выделения в одном вьюпорте, «Рассылка» из футера в шапку таблицы, лейбл «Выручка TON» внутрь бокса, секционные лейблы сайдбара slate-400 (шелл, распространяется на все страницы — взять в пачку мелких). Живой batch-продление не кликался (деструктив на реальных данных); caps/trunk-метки не видны (данные малы, не truncated).

## Бэклаг Billing закрыт (волна 10, коммиты aff3683 + 28ef98b + e0da0ce, миграции 20260914191000/20260914192000)

**P0 найден оркестратором до код-ревью и закрыт сразу:** tariffs/tariff_bundle_items/invoices стояли с выключенным RLS при полных грантах anon/authenticated (вплоть до DELETE/TRUNCATE) — а фронт «Кассы» пишет их напрямую anon-клиентом, мимо бэкенда. Проверено на живом проде: аккаунт A видел тарифы аккаунта B как свои. Миграции: RLS owner-only на 4 таблицы (+payment_events). site-v2 эти таблицы напрямую не читает — публичные флоу через бэкенд, не задеты. Бэкенд на service-ключе — не затронут.

**Разгадка «пропавших тарифов»:** оба тарифа в проде («товар» RUB-легаси + TON-близнец, без канала) принадлежат второму auth-аккаунту (quraishi…) — тестовые артефакты из старого бэклога. После RLS оператор честно видит 0 своих тарифов; инвойсы без tariff_id (2 легаси-RUB) сознательно невидимы для owner-UI (политика через tariffs).

code-reviewer: REQUEST_CHANGES — топ-3: [P1] fail-open вебхук кассы (без секрета активация подписки бесплатно по известному memo), [P1] мутации тарифов только на RLS без .eq(owner_id), [P1] двойное создание платного тарифа (мёртвый creating-проп). Проверено по живой БД: секретов webhook ни у кого нет, провайдер не сконфигурирован → fail-closed ничего живого не ломает.

Закрыто (28ef98b): fail-closed 403 + TOCTOU-claim вебхука (условный UPDATE .neq('status','paid')); секреты убраны из GET/POST /api/payment-settings и /health (webhook_secret_set boolean); серверная валидация кошелька UQ/0Q/EQ + tg_id + проценты; creating-гард + owner-фильтры всех мутаций + локальный refresh через onChanged (useBotTariffs.reload) вместо reload-страницы; живой undo bundle-итемов; честный журнал крипты (реальный словарь event_type сверен с grep всех писцов: ton_manual_confirmed/check, admin_approved/rejected, receipt_uploaded, invoice_completed; дедуп по invoice_id — ручной TON-чек пишет два paid-события); PrioritySignalsGrid на Tailwind (был голый текст — классы не существовали ни в одном CSS).

Ревью диффа поймало 2×P1 моего среза: (1) legacy-фолбэк POST сломан select'ом publicColumns на схеме без billing_-колонок → узкий legacy-список; (2) бейдж суммы задваивал ручные TON-платежи → дедуп. Плюс: onChanged-падение врал «Не удалось создать» (refreshList с catch), 0Q-кошелёк принят сервером (placeholder UI обещал), активация-после-оплаты при падении пишет activation_failed/wait_admin в журнал (иначе ретрай провайдера уже никогда не довыдаст доступ), webhook-контракт задокументирован в backend/README.md.

Дизайн-критика: **ACCEPT** сходу (0 blocker/major, 7 minor) —(empty-состояния после RLS признаны нормой данных). Полировка миноров сходу (e0da0ce): max-w на предупреждении реквизитов, emerald-700 тост, slate-600 микро-лейбл danger на red-50 (4.35→6.93:1), ё-унификация, единые рецепты h3/икон-чипов, пустое состояние журнала в домовой dashed-грамматике.

Residual (не блокируют): секрет в query вебхука (осознанно, README), timingSafeEqual для секрета (пре-экзистинг), mark→activate порядок вебхука смягчён журналированием но не атомарен, UI-реактивации отключённых тарифов нет (сознательно; reactivate в контроллере был мёртвым кодом и до волны), .priority-grid мёртвый CSS в app.css, сайдбар-микролейблы slate-400 2.62:1 (шелл, в пачке мелких), клик-тесты создания тарифа на живых данных не делались (деструктив).

## Бэклаг Treasury закрыт (волна 11, коммиты 4a1af5f + aebce3e, миграции 20260915001000/20260915002000)

code-reviewer: REQUEST_CHANGES — 3×P1, все «в сторону вывести больше, чем есть»: (1) TOCTOU-гонка параллельных POST /treasury/withdrawals (оба видят available X, оба вставляются); (2) network_fee_ton принимался из body (отрицательная комиссия уменьшала списание и отравляла ledger); (3) fail-open проглатывание ошибок по подстроке в 6 лоадерах (ошибка чтения → нули → лимит растёт). Плюс 7×P2: мёртвая статус-машина, toFixed(4) округлял лимит вверх, AA slate-400, reqId-гард, note не рендерился без капа, бейдж без «50+», чтение файла мнемоники на каждый GET.

Проверки Needs оркестратора: **profiles имеет только SELECT-политику** — self-update роли невозможен, самозскалация requireProjectAdmin закрыта RLS; **role=admin ровно один** (оператор) — казна не смешивает деньги тенантов.

Закрыто (4a1af5f): RPC `create_project_treasury_withdrawal` (security definer + pg_advisory_xact_lock по hashtext, revoke от public/anon/authenticated — урок referral-волны); комиссия — серверная константа NETWORK_FEE_TON=0.05, клиентское значение игнорируется; 42P01-паттерн вместо подстрок, fail-closed (ошибка чтения → 500, никогда не показывать лимит по частичным данным); note кап 500; withdrawalsTruncated. Фронт: честная статус-легенда (живёт только «Запрошена» + приписка про ручную отправку), floor-лимит, inline-валидация перебора с максимумом с учётом комиссии, reqId-гард, рендер note, «50+», «Кошелёк не синхронизирован», inputMode=decimal.

**Ревью диффа поймало P0 в моей RPC-арифметике**: p_available_ton приходит УЖЕ очищенным от pending, а RPC прибавлял текущий pending к дебету ещё раз → после первого же вывода лимит «застревал» навсегда (статусы кодом не двигаются, pending монотонно растёт). Фикс (20260915002000): роут прокидывает p_pending_ton_seen, RPC сравнивает ПРИРОСТ pending (текущий − seen) + дебет новой заявки vs available — гонка закрыта (параллельная вставка раздувает v_pending сверх seen), без двойного счёта. Плюс: POST отвечал pre-call summary → фронт переведён на авторитетный reload(); network_fee_ton убран из body фронта; честный фолбэк неизвестного статуса (сырой label вместо маскировки под «Запрошена»); текст overLimit называет максимум с учётом комиссии; maxLength 500 на заметке.

Дизайн-критика: **ACCEPT** (0 blocker; 2 major «на спринт» + 3 minor дожаты сходу в aebce3e): amber-600 → amber-700 на «Баланс недоступен» (3.19→5.02:1), красный «−0.0500 TON» в покое формы убран («Останется доступно: —» до ввода), rose→red баннер, «Комиссия сети ≈ 0.05», единая формула кошелька, ё-унификация.

Residual (не блокируют): «стена из 14 нулей» в разбивке Деньги — при росте сумм просится группировка доходы/обязательства; статус sent/confirmed ставится только вручную через БД (отметка об отправке — руками в Supabase); чтение мнемоники на каждый GET и двойной summary — перф-задача с кэшем снапшота; живой клик-тест создания вывода не делался (деньги платформы, деструктив); timingSafe-сравнения секрета нет (казна секрет в запросах не использует).

## Бэклаг Broadcast закрыт (волна 12, коммиты b77eab7 + 8f211b5, миграция 20260915003000)

**P0 найден и закрыт оркестратором до фиксов:** все 4 broadcast-таблицы (campaigns/deliveries/preparations/preparation_items) с RLS OFF при полных грантах, а фронт читает broadcast_deliveries из браузера по campaign_id без owner-фильтра — кросс-тенант-лик чужих доставок (TG ID, тексты). Миграция 20260915003000 (owner-only; items — через джойн preparations, SQL-копия backend/sql/broadcast-tables-rls.sql). Фронту добавлен belt-and-braces .eq('owner_id').

code-reviewer: REQUEST_CHANGES — [P1] вся массовая отправка крутилась внутри одного HTTP-запроса (200 получателей ≈ 17 мин, сокет умирает, ретрай = вторая полная рассылка, кампания навсегда 'sending', финал не в finally, ошибки insert доставок игнорировались); [P1] на DM-поверхности CampaignReplies нет обязательного предупреждения про общий чат (правило AGENTS.md); [P2×7] flags API не читался фронтом (403 только на последнем шаге), гонки CampaignReplies/CampaignDetail, AA slate-400 системно через broadcast/ui.jsx, confirm без объёма, сырые enum-статусы, skipped_unreachable невидим, ошибки выглядят как «пусто».

Закрыто (b77eab7): **mark-and-queue** — POST /send валидирует как раньше, создаёт кампанию 'queued' + все доставки upfront 'pending' (батчи 500, сбой → failed с guard .eq('status','queued')), отвечает мгновенно; фоновый раннер backend/jobs/broadcast-delivery.job.js (setInterval 30с по стилю репо, re-entrancy guard, claim условным update, reaper зависших по meta.delivery_heartbeat_at — колонки updated_at в таблице нет); логика доставки вынесена дословно в backend/services/broadcast-delivery.service.js; юзерботы кампании перечитываются на момент доставки (проданные/pending_activation/мёртвый прокси — исключены, правило активов); прогресс в meta после каждой доставки; финал 'sent'/'completed_with_errors' по фактическому состоянию БД в try/finally. Фронт: DM-предупреждение по эталону, amber-баннер + ранний disable по флагу, тост с queued_count + автополл 15с с прогресс-баром, reqId-гарды, AA-каскад, confirm «Допишемся по ЛС: X получателей», русские статусы, skipped_unreachable, ErrorNote+«Повторить».

Ревью диффа поймало P1: **гонка джобы со вставкой доставок** — тик между созданием 'queued' и завершением вставок видел pending=[] и финализировал «отправлено 0», остаток батчей лег бы в мёртвую кампанию навсегда. Фикс: finalizeCampaign при пустых строках и meta.total>0 ждёт следующего тика + guard .eq('status','queued') на аварийном failed-апдейте. Плюс: комментарии «mark-before-send» заменены на честный send-then-mark (краш между отправкой и маркировкой = дубль одному получателю при резюме — осознанный трейд-офф), RLS-SQL закоммичен в backend/sql, флаги дефолт false (fail-safe), остаточный slate-400.

Дизайн-критика: **ACCEPT** (0 blocker; 2 major + 3 minor дожаты в 8f211b5): плейсхолдер селекта «Выбери базу» выглядел как выбранное значение (slate-900) → slate-400 в пустом состоянии + постоянные микро-лейблы НАЗВАНИЕ/БАЗА; пустой холст под шагом «База» → панель «Что дальше» (шаги 2-4); хинт под disabled CTA «Выбери базу — кнопка разблокируется»; StatTile тоны 600→700 (3.38:1→проходит); счётчик рейла «2 / 4» slate-400→slate-500 (2.62→4.76:1 — шелл, первый кусок).

Residual (не блокируют): send-then-mark дубль при краше (документирован), глобальная сериализация очереди между владельцами (одна кампания за раз; при росте — round-robin по owner), SIGTERM-хендлер для сброса claim на деплое (сейчас 10-мин заморозка очереди), имя кампании с секундами, бейджи queued/sending вживую не видны (в проде одна завершённая кампания), живой запуск рассылки не тестировался (массовый DM — деструктив; первый реальный прогон смотреть по broadcast_deliveries и прогрессу meta).

## Бэклаг Userbots + Sales-bot закрыт (волна 13, коммиты 73bd5f8 + 8a6d714)

Два параллельных код-ревью (страницы ~7800 строк). /sales-bot: **APPROVE** без P0/P1 — owner-дисциплина sales-contour.service образцовая. /userbots: **REQUEST_CHANGES** — 4×P1: (1) runtime ReferenceError («Вступить» по инвайту: POST проходил, потом падение на мёртвых setScanStartedAt → ложный тост об ошибке, центр не перезагружался); (2)+(3) утечки GramJS-клиентов без disconnect (ops-center скан при ошибке health; import-session-file — весь путь без try/finally); (4) заблокированного юзербота можно опубликовать в Shop (shop.routes не смотрел runtime_status). P2: qr-status 'failed' → 'pending' (ошибка не доходит до UI), qrSessions без TTL (брошенный QR держит клиент бессрочно), гонки центра, mark-read тянул полный Telegram-скан, AA-кластер.

Закрыто (73bd5f8): disconnect в finally на обоих путях (сохранённый клиент передаётся через null перед break); гейт публикации restricted/pending_activation + расширен на expired/dead_proxy (мёртвую сессию нельзя передать покупателю; NULL-легаси пропускается сознательно); qr-status failed аддитивно + sweepStaleQrSessions(20 мин); фронт: удалены мёртвые таймеры, reqId-гард центра (устаревшие ответы + чужой бот отбрасываются), handoffLoadedBotRef (смена бота перезагружает), mark-read scan:false (кэш вместо getDialogs), AA, FileUploadBox статичные тоны. Sales-bot: re-add существующего бота мержит admin_tg_ids и больше не ротирует webhook-секрет, detach тарифов с owner-фильтром, webhook-статус-блок всегда (neutral/ok/error, честные «апдейтов ещё не было»), dirty-тост «Проверить права», .catch на fire-and-forget, «тарифты», удалён мёртвый SalesContourSection.jsx (364 строки) + мёртвые возвраты контроллера.

Ревью диффа: **APPROVE**, дожаты 2 P2: qr-failed хендлер в pollQrStatus (иначе фикс инертен), reregister-кнопка в нейтральном блоке; подтверждено: mark-read теперь читает кэш вместо полного скана.

**Дизайн-критика ACCEPT по обеим + пойман M1-уровня UX-баг:** селекция sales-bot застревала в 'new' — инициализационный прогон эффекта (accounts ещё пусты) ставил 'new', а гард «юзер выбрал нового» держал его forever: страница /sales-bot НИКОГДА не показывала подключённого бота (у оператора бот с webhook=receiving существовал и не рендерился). Фикс (8a6d714): sawBotsRef отличает инициализацию от явного выбора. Verified на проде: бот + «Бот получает сообщения» + обе кнопки рендерятся. Дожаты: шелл-тоны (группы сайдбара, футер, «Без лимитов» — slate-400→slate-500), disable «Разлогинить остальные» до загрузки сессий.

Residual: платформенный кошелёк в shop vs правило AGENTS.md «seller's own wallet» — код документирован как осознанное решение, решение владельца (обновить AGENTS.md или вернуть seller-wallet); авто-сохранение контура при открытии страницы (продуктовое); дублирование лота в «Выставлены в Shop» и «Витрине» (одна сущность, два ракурса — просится связка); heuristics-классификация ошибок send-message по подстрокам; onboarding открыт по умолчанию при живых аккаунтах (m3, продуктовое); createAuthorizedClient может кинуть после конструирования клиента (пре-экзистент, сервисный фикс отдельным слайсом); qr-сессии до 20 мин живут.

## Бэклаг Proxies закрыт (волна 14, коммиты b2dcf31 + ef83284, миграция 20260915004000)

code-reviewer: REQUEST_CHANGES — 3×P1: (1) DELETE занятого прокси не проверял tg_accounts (FK ON DELETE SET NULL молча оставлял юзербот без прокси; пробный путь проверял, не-пробный нет; confirm на фронте обещал проверку, которой не было); (2) check/:id возвращал username/password прокси в JSON (клиент ответ не читал — чистая утечка); (3) гонка provisionManagedProxyBatch (load→save без лока, параллельный батч терял записи). 8×P2: мёртвый edit-флоу, дубли host:port при добавлении, bind-proxy TOCTOU, кулдаун check, полл-ошибка стирала список, AA slate-400 + password type=text + деньги не slate-900, warming_up по подстроке, листинг прокси с живым юзерботом.

Проверки Needs: FK = SET NULL; уникальных индексов не было; живая БД чиста (0 дублей, 0 двойных привязок) → миграция 20260915004000: unique tg_accounts(proxy_id) where not null (1:1 на уровне БД, TOCTOU bind закрыт) + unique proxies(owner_id, host, port).

Закрыто (b2dcf31): DELETE-гард с 400 «сначала перепривяжи»; check/:id без кредов; withStateLock promise-очередь вокруг provision/release/restore/reconcile; маскирование кредов всем ролям (edit-флоу мёртв — plaintext не нужен) + keep-as-is в апдейте (пустой/замаскированный username и пустой password = не затирать); дубли 23505 → дружелюбный 400; кулдаун 10с; is_pending аддитивный флаг вместо парсинга текста; гейт листинга userbot_count>0 в обоих путях + фронт-зеркало; фронт: per-row checking disable, reqId-гарды, полл-ошибка не стирает список, AA, password new-password, удалён мёртвый editProxy.

Ревью диффа: APPROVE, дожаты 4 однострочника (ef83284): provisionManagedProxy под локом, saveState атомарная запись (temp+rename), autoComplete=new-password (иначе автозаполнение браузера молча затирало креды через keep-as-is дыру), опечатка.

Дизайн-критика: REJECT (1 blocker + 5 major) → фиксы (ef83284): «Свободен» emerald-700 (3.77→5.48:1), amber/red бейджи 700 на 50-фонах, дуотон акцентов (blue действия + violet инвентарь) вместо трёх, инлайн-двухшаговое подтверждение удаления вместо window.confirm (занятый прокси — честный тост вместо ложного confirm), лейблы форм одной анатомии, статус-точки через STATUS_DOT_HEX карту, «Проверен» значение slate-700, TON в лотах slate-900, ё/aria-label. Финал рендера чистый, 0 ошибок. Полный ACCEPT-повтор не гонялся — все пункты критика однозначные класс-свапы, закрыты по его же списку (прецедент полировки после ACCEPT).

Residual: TOCTOU на DELETE-гарде (окно один roundtrip, можно триггером в БД при желании); листинг «Свободен» для прокси, занятых продажей (в документации);nested креды прокси в ответах loadOwnedUserbot/loadLatestUserbot (select * + proxies(...) — та же утечка класса, отдельное решение); warming-бейдж и Trial-вид не проверялись вживую.

## Бэклаг Autopost закрыт (волна 15, коммит 0478487 + c5774c8 + миграция 20260915220000)

code-reviewer: REQUEST_CHANGES — 3×P1: (1) DELETE бота не гасил polling — Telefaf-инстанс вечно отвечал юзерам ошибками до рестарта; (2) mark-after-send без claim/lease — каждый депой (pm2 reload) мог дать ДУБЛИ постов в каналы; (3) альбомы молча теряли кнопки лидогенерации (sendMediaGroup не принимает reply_markup). P2×11: FLOOD_WAIT убивал посты навсегда, GET /bots отдавал сырой bot_token, callback-хендлеры без bot_id-фильтра, гонки фронта, «точное время» vs тик 5 мин, мёртвый код, unbounded select, a11y тумблеров/модалки.

Закрыто (0478487): stopBot перед удалением; **claim/lease**: UPDATE status='sending' WHERE status='scheduled' перед отправкой, финальные апдейты guarded, recovery зависших 'sending' (lease = updated_at через триггер set_updated_at, 10 мин) → 'queued' в stuck-editing job; альбомы — editMessageReplyMarkup на messageIds[0] в обеих ветках sendMediaGroup; FLOOD_WAIT → возврат в 'scheduled' с retry_after; токен замаскирован (token_masked/has_invite_secret) — фронт ходит мимо роута через supabase-js, PATCH читает токен из БД сам; bot_id-фильтры + maybeSingle в колбэках; мёртвые getDueItems/markPosted/markFailed/addItem удалены (MCP использует addPostItem/publishItem); items limit(500)+total; a11y тумблеров/Copy/модалки (focus trap — residual).

**Попутная находка сабагента, подтверждена**: констрейнт autopost_items_status_check не допускал 'editing' — существующий флоу «Изменить текст» МОЛЧА ПАДАЛ В БД на проде. Миграция 20260915220000 расширила CHECK (verified, convalidated).

Ревью диффа: REQUEST_CHANGES, дожато: (1) success-UPDATE в publishItem без guard — введён флаг {claimed:true} из scheduler (blanket-guard сломал бы post_now/MCP/предложку, публикующие без claim); (2) collapseQueue с bot_id первого попавшегося → группировка пар (bot_id, канал); (3) триггер lease верифицирован по pg_trigger (BEFORE UPDATE, bump updated_at); + маскирование PATCH/init, getStats считает sending, maskBotToken харднинг, post_now reject при posted/sending.

Дизайн-критика: REJECT (3 major) → фиксы (c5774c8): пикер ботов скрыт при нуле ботов (выглядел как обрезанная CTA «Подключить новог|»), w-240 при наличии; мера строк max-w-[70ch] ×3; API-карточка свёрнута в disclosure «Интеграции и API» (инженерный контент не между админом и токеном — правило онбординга); disabled-CTA без аффорданса нажатия (shadow-none/slate); ё. Итог: чистый прод-рендер, 0 ошибок; minors (focus trap модалки, безымянные time-инпуты/корзины) — residual.

Residual: focus trap модалки; time-инпуты/иконочные корзины без имён (подключённое состояние); FLOOD_WAIT на ручной публикации → failed (осознанно); первый живой прогон claim/recovery смотреть по логам scheduler (post_published/publish_flood_wait/item_claim_lost).
