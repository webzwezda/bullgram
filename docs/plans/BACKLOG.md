# BACKLOG — единый живой список открытых задач

Ведётся с 2026-09-16. Правило: любой новый открытый пункт попадает сюда, план в docs/plans закрывается секцией «Ревью» (как в 2026-09-15-unified-design-system.md). Старые бэклоги-россыпи помечены SUPERSEDED и сверены с кодом.

## 1. Мини-волна: mono-шрифт и preload на site-v2 — ЗАВЕРШЕНО (153ee1d, деплой 2026-09-16)
**Решение.** site-v2 использует `font-mono` в 5 местах, и все — платёжные (memo, адрес кошелька, секрет счёта). Токен `--font-mono` уже отдаёт стек JetBrains Mono, но шрифт на сайте не подключён → рендер по локально установленному или ui-monospace. Решение то же, что в admin (волна 6b): копия двух woff2 в `public/fonts/` + алиасные @font-face с `/` (base сайта корневой) + preload Manrope ×2 в index.html (CLS на холодной загрузке).
**Приёмка:** билд зелёный; ratchet поймал +2 font-family (литеральные имена в @font-face — var() запрещён спекой) → осознанный baseline; на проде JBM @font-face зарегистрированы, woff2 200, preload ×2 в HTML. JBM не preloadится сознательно — нужен только на платёжных экранах (~52 КБ на остальных страницах лишние).

## 2. access_requests admin-поверхность — ОТСРОЖЕНО (триггер не сработал), защита проверена
**Факты (2026-09-16):** заявок 0; RLS включена, единственная политика `access_requests_service_role_all` (ALL только для service_role) — аноним/тенанты ПИИ не видят, запись через бэкенд с service key. Риск закрыт на уровне базы.
**Решение, когда придёт первая заявка:** НЕ мульти-тенантная страница. Это лиды платформы, не тенантные данные → read-only страница `/app/access-requests`, видимая только платформенному владельцу (gate по email владельца в AuthGate/профиле), кнопка «обработано». Час работы.
**Кто:** оркестратор по команде владельца.

## 3. border.strong и WCAG 1.4.11 — ЗАКРЫТО АНАЛИЗОМ (значение не меняем)
**Решение.** 1.4.11 требует 3:1 для границы только когда она — единственный визуальный индикатор контрола. В нашем ките контролы залиты (input `bg-slate-50`, кнопки filled) и несут focus `ring-3` — граница никогда не единственный индикатор. FAIL kit-валидатора консервативнее WCAG; он уже толерируется гейтом по точной метке. Менять slate-300 глобально = видимая дельта против де-факто.
**Действие:** абзац-резолюция в design/README.md (сделано при создании бэклога). Пересматривать, только если появятся бордер-единственные контролы без заливки.

## 4. Легаси ~5300 raw-находок → семантика — МЕХАНИКА ДОКАЗАНА ПИЛОТОМ, расширение по триггеру
**Пилот (2026-09-16, e4ffa39):** CustomersPage — 145 замен по таблице (ink/surface/border/action/feedback, включая прозрачности и hover-варианты), все 17 пар обнулены; пиксельный diff прод-скриншотов — дельта только в зоне счётчиков (осознанная правка +1px); baseline **5300 → 5167**. Критик ACCEPT.
**Решение.** Big-bang не делаем. Тюнинг значений уже работает через примитивы (slate-* алиасятся на DTCG с волны 3), семантика добавляет только интент. Codemod по таблице из пилота механически воспроизводим на любой странице (волна = страница → замена → билд → деплой → пиксельный diff → --update-baseline); триггер — крупный редизайн/тюнинг темы или касание страницы.
**Правило сейчас:** каждое касание страницы — переводим затронутые классы на семантику + `--update-baseline`. Новые экраны — сразу на семантике (правило DESIGN.md).
**Остаток без пар в v1 (осознанный):** text-slate-600/300/800, bg-white, bg-indigo-100, ring-*, градиентные хвосты — каждой нужен новый токен (MINOR) или навсегда raw; список на примере CustomersPage зафиксирован в отчёте пилота.

## 5. Гигиена старых бэклогов — ЗАКРЫТО (2026-09-16)
**Результат:** полный верифпроход по символам кода — salesbot-review 13/13 и customers-dead-code 5/5 = ВЫПОЛНЕНО (доказательства вписаны в строки файлов), НЕ ВЫПОЛНЕННЫХ нет, переносить в BACKLOG нечего. Один нюанс на заметку: централизованный frozen-литерал api_id/api_hash в userbot.service.js:27-33 (пункт выполнен через fallback конструктора; вынос в env — отдельное решение, если когда-нибудь понадобится).

## 6. Покрытие проверками — процесс + владелец
- Процесс: видимые волны отдают критику мобильный кадр (390px) и hover-кадры ключевых контролов. Вписано в чек-лист волны.
- Владельцу (агенту недоступно): TON e2e на реальных деньгах; смоук логина PKCE при следующем входе; og:image превью в Telegram-черновике.
- Рантайм-наблюдение retention (из бэклога 14.09): нужен живой подписчик в 24ч-окне — напоминание ровно раз в цикл, `payload->>parse_fallback` на кастомных текстах. Триггер: первый подписчик, подошедший к дате истечения.
- Операционные привычки владельца (чек-лист из июльского 03-documentation.md, агент не исполняет): отдельные токены на интеграцию, скоуп read-only по умолчанию, ротация 90 дней, недельный взгляд в `/app/claw/log`. Сам 03-documentation.md — исторический план (docs-site в проде, строится деплоем), не активный бэклог.

## 7. Мелкая полировка — ОППОРТУНИСТИЧЕСКИ
- ~~Admin command center: line-through на зелёных заголовках~~ — ЗАКРЫТО КАК НЕ ПРИМЕНИМО (2026-09-16): Command Center владелец велел держать заготовкой, полировка заготовки не делается
- Счётчики бейджей табов (customers) → ЗАКРЫТО В ПИЛОТЕ (text-[10px]→[11px]; критик: «прежнее замечание о читаемости снято, строка не поехала, ACCEPT»)
- Пустая полоса ~180px под CTA лендинга → нужно решение по соц. доказательству (к владельцу: что показать — счётчик юзерботов, отзыв, скрин?)
- JBM/preload-хвост для admin закрыт в волне 6b; для site — п.1 этого списка

## 8. Инфраструктура — послефикс деплоя
- Фикс PREV_HEAD (11142cf) сработал в боевом режиме? Первое реальное добавление зависимости после фикса проверит триггер install — при следующем dep-add смотреть в лог «==> npm install admin-v2/site-v2»
- axe-гейт держится на symlink в npx-кэш кита (честный SKIPPED при eviction) — версионированный playwright в кит, когда будем рядом

## 9. Autopost «Перейти к обсуждению» — операционка и проверки после деплоя (2026-09-16)
**Состояние.** Фича задеплоена (27075dd): форвард постов в привязанную группу обсуждений закрывает отсутствие нативной кнопки у Bot API-постов. Миграция 20260916150000 применена, тумблеры выключены (manual-by-default). Детали: docs/plans/2026-09-16-autopost-discussion.md.
**Действия.** (1) Эрик: добавить `bulrun_ru_poster_bot` админом в привязанную группу обсуждений bullrun.ru → в /app включить тумблер «Обсуждение» у канала (бэкенд сам провалидирует и подскажет текстом, если бот не в группе). (2) Критик-проход по новому тумблеру на прод-скриншотах (нужен браузер владельца): мобильный кадр 390px + hover, по standing rule — после код-ревью и деплоя, оба уже есть. (3) e2e: у ближайшего планового поста (10:00) должна появиться кнопка «Перейти к обсуждению»; rolling-pin через MCP message_delete должен чистить и копию в обсуждении.
**Владелец:** Эрик (операционка), оркестратор (критик/e2e-проверка).
**Триггер:** следующий плановый пост или следующий визит владельца с подключённым браузером.

## 10. webhook_status официальных ботов не гаснет при молчании (2026-09-16)
**Наблюдение.** `tg_accounts.webhook_status='receiving'` остаётся, хотя `last_update_at` трёхнедельной давности (кейс Pay_Virtual_Goddesses_bot: токен отозван на стороне Telegram, вебхук молчит с 24.08, а статус врёт «receiving»). Админ видит зелёный статус и мёртвого бота.
**Решение.** Считать статус производным: при отсутствии апдейтов дольше N часов (порог — решение владельца, например 24ч) показывать «нет апдейтов» в UI, а не доверять записанному webhook_status. Либо обновлять статус из монитора прав / крон-проверки getWebhookInfo.
**Владелец:** оркестратор → фронт/бэк слайс по триггеру. **Триггер:** следующее касание official-bot UI/монитора.

## 11. bot-rights-monitor: горизонт масштаба (2026-09-16)
**Состояние.** Прогон строго последовательный (1с между запросами), guard от наложения тиков добавлен (вкл. первый прогон). При 1000 контуров × до 4 площадок ≈ 80–90 мин на 12ч-цикл — безопасно. При ~10k контуров прогон превысил бы интервал и монитор стал бы занят постоянно.
**Решение (когда сработает триггер).** Батчирование по owner_id с распределением по часам или вынос в очередь; метрика длительности прогона в лог.
**Владелец:** оркестратор. **Триггер:** >2000 контуров в sales_bot_contours или длительность прогона >1ч в логах.

## 12. Универсальный роутер исходящих сообщений — ЗАКРЫТО (a61f440, 2026-09-16)
**Результат.** Реализовано по плану docs/plans/2026-09-16-messaging-router.md (ревью-секция там же): ядро-роутер с квотами/паузами/ротацией + леджер userbot_send_log, /api/messaging (capacity + send), все юзербот-ЛС через роутер (broadcast, retention с фиксом контурного пула и common_chat_id, auto-kick, abandoned-фолбэк за флагом), стоп кампаний, peer-invite join от юзербота-админа, actor_rights_by_target в /contours, UI: предупреждение ёмкости + «Стоп» + чипы прав на /app/bases. Миграции 20260916210000/220000. Тесты 199+ ассертов (test:messaging/broadcast/lifecycle/contours + расширение test:sales). Код-ревью: 3 P1 + 4 P2 исправлены до пуша.

## 13. Роутер — хвосты наблюдаемости (2026-09-16)
**Паузы юзерботов не видны владельцу.** `tg_accounts.dm_paused_until/dm_pause_reason` пишутся роутером (flood → ~1ч, spam/флаг → 24ч), но ни один экран не показывает «юзербот на паузе до …». Владелец видит это косвенно: ёмкость на шаге «Юзерботы» считает по элигибельным, кампания с пустым пулом падает с queue_error.
**Решение.** Бейдж «на паузе до HH:MM (причина)» в списке юзерботов /app/userbots + в блоке пула на шаге «Юзерботы» рассылки; данные уже в tg_accounts. Мелочь: `getActorPause` в роутере читается без owner-скоупа (в отличие от pauseActor/clearPause) — доунифицировать при касании.
**Владелец:** оркестратор → фронт/бэк слайс. **Триггер:** первая жалоба «почему юзербот не шлёт» или следующее касание /app/userbots.
**Тоже в хвосте:** prod-проход по браузеру владельца (ёмкость, «Стоп», чипы — browser doctrine); e2e первой реальной рассылки: flood у актёра → строка остаётся pending → догонка следующим тиком; warmup-профили для новых юзерботов (пониженные капы первые N дней).

## 14. Выход юзерботов из групп по окончании рассылки — ЗАКРЫТО (2026-09-17)
**Задача владельца.** После успешной рассылки вступившие-ради-охвата юзерботы выходят из групп (с админкой), «старые» админы остаются. Галочка «выйти по окончанию рассылки» на шаге «Отправка».
**Результат.** Реализовано и отревьюено: план + ревью-секция в docs/plans/2026-09-17-broadcast-membership-cleanup.md. Миграция 20260917120000. Факт join'а ради кампании пишется в broadcast_preparation_joins (marked tg_chat_id), cleanup-job самовыходом (kick-фолбэк через промоутера) после sent/completed_with_errors при галочке; свои чаты и контурные слоты — skipped_protected. Код-ревью поймало и исправило P0 (несовпадение форматов id делало протекцию мёртвой), 2×P1 (InputUserSelf в DeleteChatUser; eject «старых» админов при рестарте/упавшем скане) и 4×P2. Тесты: test:broadcast-cleanup 92 ассерта.

## 15. Автопостер-чек-листы: операционка после релиза (2026-09-17)
**Короткий контекст.** Чек-листы автопостера реализованы по плану docs/plans/2026-09-17-autopost-checklists.md (Фазы 1–4: данные+публикация+тогглы, MCP/REST-поверхность, админка и бот-меню, ленивое истечение и ретеншен events). Осталась живая эксплуатация, кодом не закрывается.
**Решение.** (1) ✅ e2e пройден 2026-09-17 живым циклом на агентской тестовой группе (группа/бот созданы юзерботом через новые userbot-операции, см. §16): create → живой тап владельца в Telegram → state с именем/временем → cancel; попутно исправлен формат chat_id у созданных групп (Bot API требует `-100…`). Тестовые артефакты удалены. (2) ✅ Секция «Чек-листы» удалена из админки (решение владельца 2026-09-18: фича AI-only, управление через Bullgram MCP/REST); вместо неё в /app/autopost добавлен read-only «Журнал публикаций» с табами Медиа/Чекбоксы/Текст — design-critic по standing rule теперь актуален для журнала (прод-скриншоты, мобильный кадр 390px). (3) Unlink канала с живыми чек-листами: прогнать жизненный путь отвязанных списков (posted-копии в отвязанном чате, queued-строки, state-чтения) и решить, что видит админ. (4) Агрегация state-фетчей в админке, если списки разрастутся (секция «Чек-листы» читает state по каждому списку).
**Владелец:** владелец продукта. **Триггер:** первый плановый чек-лист после деплоя.

## 16. Юзербот-операции: волна 3 + апгрейд GramJS (2026-09-17)
**Короткий контекст.** Волны 1–2 реализованы по плану docs/plans/2026-09-17-mcp-userbot-ops.md: жизненный цикл группы/бота (create/invite/promote/invite-link/BotFather/bot_init + фикс leave-basic) и банальные операции с сообщениями/чатами (edit/delete/forward/pin/read/user_resolve). Всё, что осознанно не вошло, живёт здесь.
**Решение.** Волна 3 (когда появится первый реальный сценарий, по одному, не пачкой): media send/download, реакции, опросы, slow mode/banned-rights, контакты/блок, профиль/аватар, basic-группы для admin-веток (паттерн `messages.EditChat*`), kick как MCP-обёртка, авторизации как MCP. Плюс отдельная задача «апгрейд GramJS»: сейчас `telegram` запинен на `2.26.22` без `^` (последняя в npm) — при появлении новой версии поднять и прогнать канарку: операции group/invite/promote (канарка — новые операции волны 2 на живом юзерботе). Отдельно: атомарная квота bot_init (select-потом-insert гонка; RPC или unique-счётчик).
**Владелец:** владелец продукта. **Триггер:** первый реальный сценарий под операцию из волны 3 или релиз новой версии GramJS.

## 17. /access-request осиротел после редизайна главной (2026-09-19)
**Что случилось.** Редизайн site-v2 главной (b0ee6b1 + фикс-коммит) убрал последний UI-линк на `/access-request` (вместо «Нужен другой тариф? Напиши» теперь SUPPORT_TELEGRAM). Роут (`site-v2/src/App.jsx`), страница и бэкенд-эндпоинт живы, но попасть туда можно только прямым URL.
**Решение.** Выбор владельца: (а) вернуть ссылку — например, строкой в футере/под тарифами «Или оставь заявку на доступ →», или (б) убрать роут+страницу+бэкенд-маршрут целиком (заявки и так = 0, см. п.2 этого бэклога — там решение «ждать первую заявку», путь (а) согласуется с ним).
**Владелец:** владелец продукта. **Триггер:** следующее касание site-v2 или решение по лид-путям платформы.
