# Customers Bot Selector - 2026-04-24

## Plan

- [x] Inspect `CustomersPage` URL/filter state and current `/api/customers/workbench` usage.
- [ ] Read `bots` from the workbench response and keep the selected `bot_id` in `searchParams`.
- [ ] Add a compact official-bot selector with an `all bots` option and deleted/inactive markers.
- [ ] Preserve tab switches, handoff reset behavior, and search filtering while carrying `bot_id`.
- [ ] Run `cd admin-v2 && npm run build`.

## Review

- Pending implementation.
